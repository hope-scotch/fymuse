# Lime Studio — optional dependencies: live microphone features
# (audio-reactive lighting driven by the real stage mix + live BPM detection).
# Without these, Lime Studio runs in simulator mode and everything else still works.
#
# pyaudio needs PortAudio installed first:
#   macOS:   brew install portaudio
#   Debian:  sudo apt install portaudio19-dev
#   Windows: wheels are prebuilt, no extra step
numpy>=1.24
pyaudio>=0.2.13
