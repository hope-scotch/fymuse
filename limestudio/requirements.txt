# Lime Studio — core dependencies (install cleanly on macOS / Windows / Linux)
flask>=3.0
flask-sock>=0.7

# Native desktop window
pywebview>=5.0
# Linux desktop also needs a GUI backend, e.g.  pip install "pywebview[qt]"

# Real DMX output to an ENTTEC DMX USB Pro (Art-Net & simulator work without it)
pyserial>=3.5

# MIDI cue triggers from a foot pedal / pad controller
mido>=1.3
python-rtmidi>=1.5

# Optional mic features (audio-reactive lighting + live BPM detect) live in
# requirements-optional.txt — they need PortAudio and aren't required to run.
